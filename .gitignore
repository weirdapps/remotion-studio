# No trailing slashes — patterns also match symlinks
node_modules
dist
out
.DS_Store

# Editor / AI assistant local configs
.claude
.vscode
.superpowers

# Render outputs
*.mp4
*.webm

# Runtime assets — populate locally, not committed
public/*
!public/.gitkeep
